//sprite size
var scale  = 1;

//sprite scaling
image_xscale = scale;
image_yscale = scale;

//color filter
image_blend = make_colour_hsv(random(255), random(255), 255);

//inits for tracking animations
stepCounter=0;
mouthCounter=0;

depth = -y;
